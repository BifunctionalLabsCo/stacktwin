# Local evaluation defaults. Copy this file to .env, then run the three commands
# in README.md. No Nebius credentials are required to explore the classroom.
STACKTWIN_APP_MODE=local
PROFILES_DIR=profiles
OUTPUTS_DIR=outputs
STACKTWIN_APP_PASSWORD=stacktwin-local
STACKTWIN_COOKIE_SECURE=false
